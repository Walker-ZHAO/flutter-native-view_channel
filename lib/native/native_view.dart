import 'package:flutter/material.dart';

///
/// 原生视图包装组件
///
/// Author: walker
/// Email: zhaocework@gmail.com
/// Date: 2022/4/20
class NativeViewWidget extends StatelessWidget {
  const NativeViewWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
